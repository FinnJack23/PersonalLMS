# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/

# Virtual environments
.venv/
venv/

# Testing & linting caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# Build artifacts
build/
dist/

# Environment files — never commit real values
.env
.env.*
!.env.example

# Local runtime databases
*.sqlite3
*.db

# Local scratch / vault test outputs
.local/
tmp/

# Observed gate output. Reviewed expectations live under tests/ and are
# committed; everything a gate run *observes* is runtime output and is
# never committed, so an observed report can never be mistaken for an
# approved golden artifact.
var/

# Claude Code local permissions
.claude/settings.local.json
