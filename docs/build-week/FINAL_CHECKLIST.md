# Final Checklist

- [x] Run Ruff check, Ruff format check, mypy, pytest, and `git diff --check`.
- [x] Run the synthetic demo on `127.0.0.1` and verify judge-visible markers.
- [x] Review the live GPT-5.6 payload and record sanitized evidence.
- [x] Run one redacted live smoke test with no secret output.
- [x] Scan for secrets, private paths, hashes, and production data.
- [ ] Ask Alan to run `/feedback` and preserve the returned Codex Session ID.
- [ ] Configure and approve a Git remote before any push or pull request.

The remaining unchecked items require external user/publication authority and are intentionally not performed by this local release task.
