# Final Checklist

- [ ] Run ruff check, ruff format check, mypy, pytest, and `git diff --check`.
- [ ] Run the synthetic demo on `127.0.0.1` and review the UI.
- [ ] Review the live GPT-5.6 payload and obtain Alan's explicit approval.
- [ ] Run one redacted live smoke test; record no secret output.
- [ ] Scan for secrets, private paths, hashes, and production data.
- [ ] Ask Alan to run `/feedback` and preserve the returned Codex Session ID.
- [ ] Review and approve repository publication before any push.
